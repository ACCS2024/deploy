#!/bin/bash
#===============================================================================
# trojan-go.sh - Trojan-Go 部署入口（兼容旧版，转发到新版模块化脚本）
#===============================================================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 转发到新的模块化脚本
exec bash "${SCRIPT_DIR}/trojan-go/main.sh" "$@"
